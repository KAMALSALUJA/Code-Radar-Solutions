#include <stdio.h>
int main() {
char word[100]; 
    printf("a single word(string\n");
    scanf("%s", &word);
    printf("print the entered string %s", word);
    //printf("%s", welcome());
    return 0;
}